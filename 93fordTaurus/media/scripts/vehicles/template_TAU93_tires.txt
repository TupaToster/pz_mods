module Base
{

	model 93fordTaurusWheel
	{
		mesh = vehicles/Vehicles_93fordTaurus_Wheel|taurus_tire,
		texture = vehicles/Vehicles_93fordTaurus_Wheel,
		shader = vehiclewheel,
		scale = 0.1,
	}

	model 93fordTaurusSHOWheel
	{
		mesh = vehicles/Vehicles_93fordTaurus_Wheel|taurus_tire,
		texture = vehicles/Vehicles_93fordTaurusSHO_Wheel,
		shader = vehiclewheel,
		scale = 0.1,
	}

	template vehicle TAU93Tires
	{
		part TireFrontLeft
		{
			area = TireFrontLeft,
			wheel = FrontLeft,
			table install
			{
				requireInstalled = BrakeFrontLeft;SuspensionFrontLeft,
			}

		}

		part TireFrontRight
		{
			area = TireFrontRight,
			wheel = FrontRight,
			table install
			{
				requireInstalled = BrakeFrontRight;SuspensionFrontRight,
			}
		}

		part TireRearLeft
		{
			area = TireRearLeft,
			wheel = RearLeft,
			table install
			{
				requireInstalled = BrakeRearLeft;SuspensionRearLeft,
			}
		}

		part TireRearRight
		{
			area = TireRearRight,
			wheel = RearRight,
			table install
			{
				requireInstalled = BrakeRearRight;SuspensionRearRight,
			}
		}

		part Tire*
		{
			category = tire,
			itemType = Base.93fordTaurusTire;Base.93fordTaurusSHOTire,
			container
			{
				capacity = 35,
				test = Vehicles.ContainerAccess.Tire,
				contentType = Air,
			}
			table install
			{
				items
				{
					1
					{
						type = Base.Jack,
						count = 1,
						keep = true,
					}
					2
					{
						type = Base.LugWrench,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 400,
				skills = Mechanics:1,
				test = Vehicles.InstallTest.Default,
				complete = TAU93.InstallComplete.Tire,
			}
			table uninstall
			{
				items
				{
					1
					{
						type = Base.Jack,
						count = 1,
						keep = true,
					}
					2
					{
						type = Base.LugWrench,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 400,
				skills = Mechanics:1,
				test = Vehicles.UninstallTest.Default,
				complete = TAU93.UninstallComplete.Tire,
			}

			model fordTaurusTire
			{
				file = 93fordTaurusWheel,
			}

			model fordTaurusSHOTire
			{
				file = 93fordTaurusSHOWheel,
			}

			lua
			{
				create = Vehicles.Create.Tire,
				init = TAU93.Init.Tire,
				checkOperate = Vehicles.CheckOperate.Tire,
				update = Vehicles.Update.Tire,
			}
		}
	}
}

